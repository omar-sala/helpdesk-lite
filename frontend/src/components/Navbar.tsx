import { NavLink } from 'react-router-dom'
import { LogOut, Ticket, User as UserIcon } from 'lucide-react'
import { useAuth } from '../context/AuthContext'
export function Navbar() { const { user, logout } = useAuth(); if (!user) return null; const base = user.role === 'MANAGER' ? '/manager' : user.role === 'AGENT' ? '/agent' : '/employee'; const link = ({ isActive }: {isActive:boolean}) => `rounded-md px-3 py-2 text-sm ${isActive ? 'bg-slate-800 text-white' : 'text-slate-400 hover:text-white'}`; return <nav className="border-b border-slate-800 bg-slate-900/90"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-4"><div className="flex items-center gap-5"><div className="flex items-center gap-2 font-bold"><span className="rounded bg-indigo-600 p-2"><Ticket className="h-4 w-4" /></span>HelpDesk Lite</div><div className="hidden sm:flex"><NavLink end to={base} className={link}>Dashboard</NavLink><NavLink to={`${base}/tickets`} className={link}>Tickets</NavLink>{user.role === 'EMPLOYEE' && <NavLink to="/employee/create-ticket" className={link}>New ticket</NavLink>}{user.role === 'MANAGER' && <NavLink to="/manager/users" className={link}>Users</NavLink>}</div></div><div className="flex items-center gap-3 text-sm"><UserIcon className="h-4 w-4 text-slate-400" /><span className="hidden sm:inline">{user.name}</span><button onClick={logout} title="Sign out" className="text-slate-400 hover:text-red-400"><LogOut className="h-4 w-4" /></button></div></div></nav> }
