import { useEffect, useState } from 'react'
import { Link } from 'react-router-dom'
import { ticketService } from '../../services/ticket.service'
export function AgentDashboard() { const [summary, setSummary] = useState<Record<string, number>>({}); useEffect(() => { ticketService.getSummary().then(setSummary).catch(() => undefined) }, []); return <section><div className="mb-6 flex items-center justify-between"><div><h1 className="text-2xl font-bold">Agent dashboard</h1><p className="text-slate-400">Your queue and unassigned requests.</p></div><Link to="/agent/tickets" className="btn-primary">Open queue</Link></div><div className="grid gap-4 sm:grid-cols-3">{[['Open queue', summary.open], ['In progress', summary.inProgress], ['Assigned to me', summary.assignedToMe]].map(([name, count]) => <div key={String(name)} className="rounded-xl border border-slate-800 bg-slate-900 p-5"><p className="text-sm text-slate-400">{name}</p><p className="mt-2 text-3xl font-bold">{count ?? 0}</p></div>)}</div></section> }
